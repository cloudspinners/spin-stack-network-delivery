instance:
  group: local
parameters:
  region: eu-west-1
  environment_name: local
  managed_stack_name: network_stack
terraform_backend:
  region: eu-west-1
  bucket: statebucket-root-network-XXXXXXXXXXXX
